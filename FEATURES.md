# 🎯 AI Support Agent — Возможности системы

> Полное описание всех функций интеллектуального агента поддержки

---

## 📋 Оглавление

1. [Интеллектуальная классификация](#1-интеллектуальная-классификация)
2. [RAG — Ответы из базы знаний](#2-rag--ответы-из-базы-знаний)
3. [Самооценка ответа](#3-самооценка-ответа)
4. [Условная эскалация](#4-условная-эскалация)
5. [Дедупликация тикетов](#5-дедупликация-тикетов)
6. [Маршрутизация по линиям](#6-маршрутизация-по-линиям)
7. [O2O — Поиск отделений](#7-o2o--поиск-отделений)
8. [Голосовой ввод](#8-голосовой-ввод)
9. [Анализ изображений](#9-анализ-изображений)
10. [Интерфейс оператора](#10-интерфейс-оператора)

---

## 1. Интеллектуальная классификация

**Файл:** `classifier.py`

При каждом обращении LLM анализирует запрос и возвращает:

```json
{
  "category": "technical|billing|account|feature|bug|other",
  "criticality": "low|medium|high|critical",
  "support_line": "line_1|line_2|line_3",
  "is_bank_related": true|false,
  "is_new_topic": true|false,
  "needs_offline": true|false
}
```

### Как это работает:
1. Запрос пользователя + история диалога → GigaChat
2. LLM возвращает структурированный JSON
3. Система использует данные для маршрутизации

### Защита от оффтопа:
Если `is_bank_related = false`, бот отвечает:
> ❗ Данный запрос не поддерживается ИИ-ассистентом Сбербанка.

---

## 2. RAG — Ответы из базы знаний

**Файл:** `rag_system.py`

### Принцип работы:
1. Запрос пользователя преобразуется в вектор (эмбеддинг)
2. ChromaDB ищет похожие документы из базы знаний
3. Найденные документы добавляются в промпт LLM
4. LLM генерирует ответ на основе контекста

### Параметры:
| Параметр | Значение | Описание |
|----------|----------|----------|
| `threshold` | 0.85 | Минимальный коэффициент схожести |
| `max_results` | 3 | Максимум документов для контекста |

### Пример:
```
Запрос: "Как оформить кредитную карту?"
         ↓
Поиск в ChromaDB → similarity = 0.92
         ↓
Контекст: "Для оформления кредитной карты необходимо..."
         ↓
GigaChat: генерирует ответ с учётом контекста
```

---

## 3. Самооценка ответа

**Файл:** `classifier.py` → `assess_response()`

После генерации ответа агент **сам оценивает**, помог ли он:

```json
{
  "is_resolved": true|false,
  "confidence": 0-100,
  "needs_escalation": true|false,
  "escalation_reason": "причина или null"
}
```

### Правила оценки:
- Ответ содержит "не могу", "обратитесь" → `needs_escalation = true`
- Вопрос о блокировке/мошенничестве → `needs_escalation = true`
- `confidence < 60` → `needs_escalation = true`

---

## 4. Условная эскалация

**Файл:** `handlers/user.py`

### Старая логика (убрана):
> Тикет создавался при каждой новой теме

### Новая логика:
> Тикет создаётся **только если агент не помог**

### Триггеры создания тикета:
1. ✅ `needs_escalation = true` (агент не справился)
2. ✅ `support_line = line_2|line_3` (сложный вопрос)
3. ✅ Пользователь нажал "❌ Проблема не решена"

---

## 5. Дедупликация тикетов

**Файл:** `escalation.py`

### Проблема:
Один пользователь может задать похожий вопрос несколько раз → создаётся много дублей.

### Решение:
При создании тикета система ищет **семантически похожие открытые тикеты**.

### Алгоритм:
```
1. Новый запрос → генерация эмбеддинга
2. Поиск в ChromaDB (коллекция active_tickets_vectors)
3. Если distance < 0.4 → запрос присоединяется к существующему тикету
4. Пользователь видит: "💡 Запрос присоединен к обращению #123"
```

### Параметры:
| Параметр | Значение | Описание |
|----------|----------|----------|
| `threshold` | 0.4 | Порог L2-дистанции для объединения |

---

## 6. Маршрутизация по линиям

**Файл:** `classifier.py`

### Линии поддержки:
| Линия | Назначение | Примеры вопросов |
|-------|------------|-----------------|
| **LINE_1** | Базовые консультации | Курс валют, график работы |
| **LINE_2** | Технические вопросы | Ошибки в приложении, интеграции |
| **LINE_3** | Экспертные вопросы | Сложные финансовые операции |

### Автоматическое распределение:
LLM анализирует сложность запроса и назначает линию.

---

## 7. O2O — Поиск отделений

**Файлы:** `geo_service.py`, `webapp/index.html`, `app_server.py`

### Что делает:
Пользователь нажимает "📍 Найти отделение" → открывается карта с ближайшими офисами Сбербанка.

### Технологии:
- **2GIS Places API** — поиск точек на карте
- **Telegram Mini App** — интерактивная карта
- **Ngrok** — HTTPS-туннель для доступа

### Фильтрация:
```python
# geo_service.py
# Исключаем банкоматы и терминалы
if "банкомат" in name.lower() or "терминал" in name.lower():
    continue  # Только отделения
```

### Кнопки в Mini App:
| Кнопка | Действие |
|--------|----------|
| 📍 | Центрировать на моём местоположении |
| 🔄 | Перезагрузить страницу |
| ➡️ (на карточке) | Построить маршрут в 2GIS |

---

## 8. Голосовой ввод

**Файл:** `salute_speech_client.py`

### Что делает:
Пользователь отправляет голосовое сообщение → система распознаёт текст → обрабатывает как обычный запрос.

### Технология:
- **Sber Salute Speech API** (STT — Speech-to-Text)

### Поддерживаемые форматы:
- OGG/Opus (стандарт Telegram)
- WAV

---

## 9. Анализ изображений

**Файл:** `gigachat_client.py` → `analyze_image()`

### Что делает:
Пользователь отправляет скриншот ошибки → LLM анализирует изображение и отвечает.

### Технология:
- **GigaChat Vision API**

### Алгоритм:
```
1. Изображение → BytesIO stream
2. Upload в GigaChat File Storage → file_id
3. Chat request с attachment → анализ
4. Ответ пользователю
```

---

## 10. Интерфейс оператора

**Файлы:** `handlers/admin.py`, `keyboards/operator_kb.py`

### Доступ:
Только пользователи из списка `OPERATOR_IDS` в `.env`

### Клавиатура оператора:
| Кнопка | Действие |
|--------|----------|
| 📋 Активные заявки | Список тикетов с кнопками |
| 📊 Статистика | Загрузка по линиям |
| 🔍 Поиск тикета | Подсказка по /ticket |
| ❓ Справка | Команды оператора |

### Инлайн-кнопки на тикете:
| Кнопка | Действие |
|--------|----------|
| 👁️ Подробнее | Детали тикета |
| ✋ Взять | Назначить себя |
| 💬 Ответить | FSM-режим ввода ответа |
| ✅ Закрыть | Завершить тикет |

### FSM для ответа:
```
Оператор нажимает "💬 Ответить"
         ↓
Бот: "✍️ Введите ответ для тикета #123:"
         ↓
Оператор пишет текст
         ↓
Ответ сохраняется в БД + отправляется пользователю
```

---

## 11. Сбор метрик и аналитика

**Файл:** `metrics.py`

### Что делает:
Система автоматически оценивает продуктивность ИИ-ассистента, отслеживая успешность решения запросов без участия человека.

### Команды:
*   `/metrics` (только для операторов) — выводит сводный отчет.

### Отслеживаемые показатели (KPI):
1.  **Total Requests**: Общее количество обращений.
2.  **AI Resolved**: Успешно решено ИИ (без эскалации).
3.  **Success Rate**: `%` автоматизации (Target: >80%).
4.  **Escalated**: Передано на линию поддержки.
5.  **Off-Topic**: Отфильтрованные "мусорные" запросы (не банк).

### Структура отчета:
```text
📊 Efficiency Metrics:
-------------------
Total Requests: 42
✅ AI Resolved: 35
⏩ Escalated: 5
🗑 Off-Topic: 2

🏆 Success Rate: 83.3%
-------------------
```

---

## 12. Adaptive Intelligence (Autopilot) 🧠

**Файлы:** `metrics.py`, `classifier.py`, `config.py`

### Что делает:
Динамическая система, которая автоматически "подкручивает" порог уверенности ИИ в зависимости от его текущей успешности.

### Как это работает:
1.  **Цель (Target):** Вы задаете целевой % автоматизации (например, 80%).
2.  **Реальность:** Система считает текущий `Success Rate` (отношение решенных к общим).
3.  **Реакция:**
    *   Если успех < 80% (бот рискует) → Порог уверенности **ПОВЫШАЕТСЯ** (бот становится строже).
    *   Если успех > 80% (бот слишком скромен) → Порог уверенности **СНИЖАЕТСЯ** (бот становится смелее).

### Формула:
`NewThreshold = CurrentThreshold - (TargetRate - CurrentRate) * Factor`

Это превращает бота в саморегулирующуюся систему, которая всегда стремится к заданному KPI.

---

## 📊 Схема работы системы

```
┌─────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ                              │
│   Текст 📝 │ Голос 🎙️ │ Фото 📷 │ Локация 📍                │
└──────┬──────────┬──────────┬──────────┬─────────────────────┘
       │          │          │          │
       ▼          ▼          ▼          ▼
┌──────────────────────────────────────────────────────────────┐
│                     CLASSIFIER                                │
│  classify() → category, criticality, line, is_bank_related   │
└──────────────────────────┬───────────────────────────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
         is_bank?      RAG Search   needs_offline?
              │            │            │
              │     ┌──────┴──────┐     │
              │     ▼             ▼     ▼
              │  ChromaDB    GigaChat  O2O Map
              │     │             │     
              │     └──────┬──────┘     
              │            ▼            
              │      ОТВЕТ + assess_response()
              │            │
              │   ┌────────┴────────┐
              │   ▼                 ▼
              │ is_resolved?    needs_escalation?
              │   │                 │
              │   ▼                 ▼
              │ Кнопка          Создать
              │ "Не решено"     ТИКЕТ
              │                    │
              │                    ▼
              │            ┌───────────────┐
              │            │ ОПЕРАТОР      │
              │            │ /tickets      │
              │            │ /reply        │
              │            └───────────────┘
              │
              ▼
         ОТКЛОНИТЬ (оффтоп)
```

---

## 🔧 Конфигурация

Все настройки в файле `.env`:

```env
# LLM
GIGACHAT_AUTHORIZATION_KEY=your_key

# Telegram  
TELEGRAM_BOT_TOKEN=your_token
OPERATOR_IDS=123456789,987654321

# Voice
SALUTE_SPEECH_CLIENT_ID=your_id
SALUTE_SPEECH_CLIENT_SECRET=your_secret

# Maps
DG_API_KEY=your_2gis_key
NGROK_AUTHTOKEN=your_ngrok_token
WEBAPP_URL=https://your-domain.ngrok-free.app
```

---

*Документация актуальна для версии Platinum Edition v1.0*
