# Интеллектуальный чат-бот для поддержки клиентов

Система представляет собой интеллектуального чат-бота для поддержки клиентов с использованием Giga Chat API.

> 📖 **Полное руководство по проекту**: См. [PROJECT_GUIDE.md](PROJECT_GUIDE.md) для детального описания архитектуры, работы БД и рабочих процессов.

## Возможности

- 🤖 **RAG (Retrieval-Augmented Generation)** - ответы на типовые вопросы на основе базы знаний
- 🏷️ **Классификация обращений** - автоматическая классификация по тематике и критичности
- 📋 **Создание обращений** - автоматическое создание заявок в соответствующие линии поддержки
- 📊 **Маршрутизация** - распределение обращений по трем линиям поддержки (1/2/3)
- 💾 **База данных** - сохранение всех обращений и истории взаимодействий
- 📱 **Telegram Bot** - удобный интерфейс для пользователей
- 🎤 **Распознавание голосовых сообщений** - через Sber Salute Speech (SmartSpeech REST API)
- 🖼️ **Обработка скриншотов** - распознавание текста на изображениях через Tesseract OCR

## Архитектура

### Компоненты системы:

1. **RAG System** (`rag_system.py`) - поиск релевантных ответов в базе знаний
2. **Classifier** (`classifier.py`) - классификация обращений с помощью Giga Chat
3. **Escalation System** (`escalation.py`) - маршрутизация и управление обращениями
4. **Giga Chat Client** (`gigachat_client.py`) - клиент для работы с Giga Chat API
5. **Telegram Bot** (`bot.py`) - основной бот с интеграцией всех компонентов
6. **Models** (`models.py`) - модели данных для базы данных

### Модель обращения (Ticket):

- **ID** - уникальный идентификатор
- **Заголовок** - краткое описание проблемы
- **Описание** - полное описание обращения
- **Критичность** - low, medium, high, critical
- **Линия поддержки** - line_1, line_2, line_3
- **Статус** - open, in_progress, escalated, resolved, closed
- **Категория** - technical, billing, account, feature, bug, other
- **История общения** - JSON с предыдущими сообщениями

### Линии поддержки:

- **Line 1** - типовые вопросы, базовая поддержка
- **Line 2** - технические вопросы, требуют экспертизы
- **Line 3** - сложные/критичные вопросы, требуют специалистов

## Установка

### 1. Клонируйте репозиторий или скачайте файлы

### 2. Создайте виртуальное окружение

**Важно:** Рекомендуется использовать **Python 3.11 или 3.12**. Python 3.14 может иметь проблемы с установкой некоторых зависимостей (numpy, chromadb).

```bash
# Windows PowerShell
python -m venv .venv
.venv\Scripts\Activate.ps1

# Windows CMD
python -m venv .venv
.venv\Scripts\activate.bat

# Linux/Mac
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установите зависимости

**Для Windows (рекомендуется):**

Используйте специальный скрипт установки, который исправляет проблемы с numpy:

```powershell
# PowerShell
.\install_dependencies.ps1

# Или CMD
install_dependencies.bat
```

**Или вручную:**

```bash
# Обновите pip
python -m pip install --upgrade pip setuptools wheel

# Установите numpy отдельно (только из готовых wheel-файлов)
pip install --only-binary :all: numpy

# Установите остальные зависимости
pip install -r requirements.txt --prefer-binary
```

**Если возникают проблемы с установкой numpy:**

1. Убедитесь, что используете Python 3.11 или 3.12 (не 3.14)
2. Попробуйте установить numpy отдельно: `pip install numpy --prefer-binary`
3. Если не помогает, установите конкретную версию: `pip install "numpy<2.0.0" --prefer-binary`

### 3. Настройте переменные окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Заполните следующие параметры:

- `GIGACHAT_CLIENT_ID` - Client ID для Giga Chat API
- `GIGACHAT_CLIENT_SECRET` - Client Secret для Giga Chat API
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `SALUTE_SPEECH_CLIENT_ID` - Client ID для Salute Speech (SmartSpeech)
- `SALUTE_SPEECH_CLIENT_SECRET` - Client Secret для Salute Speech (SmartSpeech)

### 4. Получение токенов

#### Giga Chat API:
1. Зарегистрируйтесь на [Giga Chat](https://developers.sber.ru/gigachat)
2. Получите Client ID и Client Secret
3. Укажите их в `.env`

#### Telegram Bot:
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен в `.env`

#### Sber Salute Speech (SmartSpeech) для распознавания речи:

1. Зарегистрируйтесь на [портале разработчика Сбера](https://developers.sber.ru/)
2. Подключите Salute Speech / SmartSpeech и получите `Client ID` и `Client Secret`
3. Укажите `SALUTE_SPEECH_CLIENT_ID` и `SALUTE_SPEECH_CLIENT_SECRET` в `.env`

## Запуск

```bash
python bot.py
```

Бот начнет работать и будет отвечать на сообщения в Telegram.

## Использование

### Команды бота:

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/my_tickets` - Посмотреть все мои обращения
- `/clear` - Очистить историю разговора

### Примеры взаимодействия:

1. **Типовой вопрос:**
   ```
   Пользователь: Как сбросить пароль?
   Бот: Для сброса пароля перейдите в раздел 'Безопасность'...
   ```

2. **Создание обращения:**
   ```
   Пользователь: У меня проблема с оплатой
   Бот: [Отвечает на вопрос из RAG] + [Создает обращение автоматически]
   ```

## Структура базы данных

База данных автоматически создается при первом запуске. Используется SQLite по умолчанию (можно изменить на PostgreSQL через `DATABASE_URL`).

Таблица `tickets` содержит:
- Все обращения пользователей
- Классификацию и маршрутизацию
- Историю общения
- Статусы и метаданные

## Настройка базы знаний

База знаний инициализируется автоматически с базовым набором FAQ. Вы можете расширить ее:

1. Используя методы `rag_system.py`:
   ```python
   rag.add_knowledge(
       text="Ваш ответ",
       metadata={"category": "technical", "tags": ["тег1", "тег2"]}
   )
   ```

2. Или напрямую через ChromaDB API

## Расширение функционала

### Добавление новых категорий:
Отредактируйте `models.py` - добавьте новые значения в enum `Category`

### Изменение логики маршрутизации:
Отредактируйте `classifier.py` - измените промпт для классификации

### Интеграция с внешними системами:
Используйте `escalation.py` для подключения к системам тикетов (Jira, Redmine и т.д.)

## Технологии

- **Python 3.11+** (рекомендуется 3.11 или 3.12, Python 3.14 может иметь проблемы)
- **Giga Chat API** - для генерации ответов и классификации
- **Telegram Bot API** - интерфейс пользователя
- **ChromaDB** - векторная база данных для RAG
- **SQLAlchemy** - ORM для работы с БД
- **Sentence Transformers** - эмбеддинги для поиска (используется `paraphrase-multilingual-mpnet-base-v2` по умолчанию)
- **Sber Salute Speech** - распознавание речи через SmartSpeech REST API
- **Tesseract OCR** - распознавание текста на изображениях

## База данных

### Инициализация

База данных создаётся **автоматически** при первом запуске бота. Файл `support.db` появится в корне проекта.

### Обновление схемы БД

Если вы обновили код и добавили новые колонки в `models.py`, запустите:

```bash
python update_database.py
```

Этот скрипт безопасно добавит новые колонки и таблицы без потери данных.

**Когда нужно обновлять БД:**
- После обновления кода с новыми полями в моделях
- После клонирования проекта на новую машину (если БД уже существует)
- При появлении ошибок о недостающих колонках

**Подробнее:** См. раздел [Когда обновлять БД](PROJECT_GUIDE.md#когда-обновлять-бд) в PROJECT_GUIDE.md

## Решение проблем

### Ошибка при установке numpy

Если при установке возникает ошибка компиляции numpy:

1. **Используйте Python 3.11 или 3.12** вместо 3.14
2. **Используйте скрипт установки:** `install_dependencies.ps1` или `install_dependencies.bat`
3. **Установите numpy отдельно:** `pip install --only-binary :all: numpy`

### Ошибка при установке chromadb

ChromaDB может требовать компилятор C++ на Windows:

1. Установите [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
2. Или используйте предсобранные wheel-файлы: `pip install chromadb --prefer-binary`

### Tesseract OCR не найден

Для работы с изображениями нужен Tesseract OCR:

1. Скачайте и установите [Tesseract OCR для Windows](https://github.com/UB-Mannheim/tesseract/wiki)
2. Убедитесь, что `tesseract.exe` доступен в PATH
3. Или укажите путь вручную в коде (если нужно)

### Проблемы с базой данных

- **БД не создаётся:** Запустите `python init_db.py` вручную
- **Ошибки о недостающих колонках:** Запустите `python update_database.py`
- **Проверка структуры БД:** Запустите `python check_database.py`

### Проблемы с распознаванием речи

Если голосовые сообщения не распознаются:

1. **Проверьте учетные данные:** Убедитесь, что `SALUTE_SPEECH_CLIENT_ID` и `SALUTE_SPEECH_CLIENT_SECRET` правильно указаны в `.env`
2. **Проверьте размер файла:** Максимальный размер голосового сообщения - 2 МБ
3. **Проверьте качество записи:** Говорите четко и громко, близко к микрофону
4. **SSL ошибки:** Если возникают SSL ошибки, это нормально для внутренних сервисов Сбера - они обрабатываются автоматически

### Проблемы с embedding моделями

Если модели sentence-transformers не загружаются:

1. **Система автоматически пробует несколько моделей** в порядке приоритета
2. **Если ни одна модель не загрузилась**, используется встроенная функция ChromaDB
3. **Модель по умолчанию:** `paraphrase-multilingual-mpnet-base-v2` (надежная мультиязычная модель)
4. **Альтернативные модели:** Система автоматически пробует другие модели при ошибках

## Лицензия

MIT

## Автор

Разработано для системы поддержки клиентов с использованием Giga Chat API

