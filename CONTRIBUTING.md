# 🤝 Руководство для разработчиков

## Структура кода

```
handlers/      # Обработчики Telegram событий
keyboards/     # Клавиатуры (Reply, Inline)
middlewares/   # Middleware (БД сессии)
webapp/        # Mini App (HTML)
```

## Стиль кода

- **Python 3.11+**
- **Type hints** везде
- **Docstrings** для публичных функций
- **Async/await** для I/O операций

## Добавление новой фичи

1. Создайте ветку: `git checkout -b feature/название`
2. Реализуйте функционал
3. Обновите `FEATURES.md`
4. Протестируйте: `docker compose up -d --build`
5. Создайте PR

## Переменные окружения

При добавлении новых переменных:
1. Добавьте в `config.py` → класс `Settings`
2. Добавьте в шаблон `manage.py` → `cmd_env_init()`
3. Документируйте в `README.md`

## Логирование

```python
import logging
logger = logging.getLogger(__name__)

logger.info("✅ Успех")
logger.warning("⚠️ Предупреждение")
logger.error("❌ Ошибка", exc_info=True)
```

## Тестирование

```bash
# Локальный запуск
python bot.py

# Docker
docker compose up -d --build
docker compose logs -f bot
```
