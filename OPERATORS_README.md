# Руководство для операторов поддержки

## Настройка операторов

1. Получите ваш Telegram ID:
   - Напишите боту [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ваш ID (число)

2. Добавьте ID операторов в `.env` файл:
   ```bash
   python setup_operator.py
   ```
   Или вручную в `.env`:
   ```
   OPERATOR_IDS=123456789,987654321
   ```

3. Перезапустите бота

## Команды для операторов

### `/tickets`
Показывает список всех открытых тикетов (статусы `open` и `in_progress`).

**Пример:**
```
/tickets
```

**Ответ:**
```
📋 Открытые тикеты (5):

🟢 #123 - Не могу войти в аккаунт...
   Пользователь: Иван Иванов | Линия: line_1

🟡 #122 - Ошибка при оплате...
   Пользователь: Петр Петров | Линия: line_2
...
```

### `/ticket <id>`
Показывает детальную информацию о конкретном тикете, включая историю ответов операторов.

**Пример:**
```
/ticket 123
```

**Ответ:**
```
🟡 Тикет #123

📋 Заголовок: Не могу войти в аккаунт
👤 Пользователь: Иван Иванов (ID: 123456789)
📂 Категория: account
⚠️ Критичность: medium
📞 Линия: line_1
📝 Статус: in_progress
👤 Оператор: Оператор Поддержки

📄 Описание:
Здравствуйте, не могу войти в свой аккаунт...

💬 Ответы операторов:

👤 Оператор Поддержки (12.12 14:30):
Проверьте правильность логина и пароля...
```

### `/take <id>`
Берет тикет в работу. Статус тикета меняется на `in_progress`, оператор назначается на тикет.

**Пример:**
```
/take 123
```

**Ответ:**
```
✅ Тикет #123 взят в работу.
Используйте /reply <id> <сообщение> для ответа пользователю.
```

**Важно:** Если тикет уже взят другим оператором, вы получите предупреждение.

### `/reply <id> <сообщение>`
Отправляет ответ пользователю по тикету. Сообщение сохраняется в базе данных и отправляется пользователю в Telegram.

**Пример:**
```
/reply 123 Добрый день! Проверьте, пожалуйста, правильность логина и пароля. Если проблема сохраняется, попробуйте восстановить пароль через функцию "Забыли пароль?"
```

**Ответ оператору:**
```
✅ Ответ отправлен пользователю по тикету #123
```

**Сообщение пользователю:**
```
💬 Ответ от оператора по тикету #123:

Добрый день! Проверьте, пожалуйста, правильность логина и пароля...
```

### `/close <id>`
Закрывает тикет. Статус меняется на `resolved`, пользователь получает уведомление о закрытии тикета.

**Пример:**
```
/close 123
```

**Ответ оператору:**
```
✅ Тикет #123 закрыт.
```

**Сообщение пользователю:**
```
✅ Тикет #123 закрыт. Спасибо за обращение!
```

### `/stats`
Показывает статистику по тикетам: количество тикетов по статусам и по линиям поддержки.

**Пример:**
```
/stats
```

**Ответ:**
```
📊 Статистика по тикетам:

🟢 open: 5
🟡 in_progress: 3
🔴 escalated: 1
✅ resolved: 12
⚫ closed: 45

📞 По линиям поддержки:
   line_1: 3 открытых
   line_2: 4 открытых
   line_3: 1 открытых
```

## Статусы тикетов

- 🟢 **open** - Тикет создан, но еще не взят в работу
- 🟡 **in_progress** - Тикет взят оператором в работу
- 🔴 **escalated** - Тикет эскалирован на другую линию поддержки
- ✅ **resolved** - Проблема решена, тикет закрыт
- ⚫ **closed** - Тикет окончательно закрыт

## Рабочий процесс

1. **Просмотр очереди:** `/tickets`
2. **Выбор тикета:** `/ticket <id>` - просмотр деталей
3. **Взятие в работу:** `/take <id>`
4. **Ответ пользователю:** `/reply <id> <сообщение>`
5. **Закрытие тикета:** `/close <id>` (после решения проблемы)

## Линии поддержки

- **line_1** - Первая линия, типовые вопросы
- **line_2** - Вторая линия, технические вопросы
- **line_3** - Третья линия, сложные/критичные вопросы

## Безопасность

- Только пользователи, указанные в `OPERATOR_IDS`, могут использовать команды операторов
- Если попытаться использовать команду без прав оператора, вы получите сообщение: "❌ У вас нет доступа к этой команде."

