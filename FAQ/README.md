# RAG Telegram Support Bot

Интеллектуальный чат-бот для поддержки клиентов с использованием RAG (Retrieval-Augmented Generation) и автоматической эскалацией обращений.

## Возможности

- 📚 Ответы на типовые вопросы по базе знаний (RAG с векторной БД)
- 🎯 Автоматическое определение тематики и критичности обращения
- 🔄 Умная эскалация: создание обращений на 1/2/3 линию поддержки
- 💬 Интеграция с Telegram

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Скопируйте `.env.example` в `.env` и заполните:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `OPENAI_API_KEY` - API ключ OpenAI
- `DATABASE_PATH` - путь к векторной БД (по умолчанию `./chroma_db`)

3. Запустите бота:

**В PowerShell (рекомендуется):**
```powershell
.\start_bot.ps1
```

**В PowerShell (через cmd):**
```powershell
cmd /c start_bot.bat
```

**В командной строке (cmd):**
```cmd
start_bot.bat
```

**Или вручную:**
```powershell
.\venv\Scripts\activate
python main.py
```

**⚠️ ВАЖНО:** В PowerShell всегда используйте `.\` перед именем скрипта!

## Остановка бота

Если нужно остановить бота (например, при ошибке "Conflict"):

**В PowerShell:**
```powershell
.\stop_bot.ps1
```

Или через cmd:
```powershell
cmd /c stop_bot.bat
```

**В командной строке (cmd):**
```cmd
stop_bot.bat
```

**Примечание:** В PowerShell всегда используйте `.\` перед именем скрипта для безопасности.

## Структура проекта

- `main.py` - точка входа, запуск Telegram бота
- `bot/` - модули бота
  - `telegram_bot.py` - обработка Telegram сообщений
  - `rag_system.py` - RAG система с векторной БД
  - `classifier.py` - классификация тематики и критичности
  - `escalation.py` - система эскалации обращений
- `knowledge_base/` - база знаний
  - `faq/` - FAQ документы
  - `articles/` - статьи и документация
  - `tickets/` - примеры обращений
- `data/` - тестовые данные и диалоги

