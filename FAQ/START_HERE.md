# 🚀 Быстрый старт - Начните здесь!

## ⚠️ ВАЖНО: Синтаксис для PowerShell

В PowerShell **ОБЯЗАТЕЛЬНО** используйте `.\` перед именем скрипта:

```powershell
.\start_bot.ps1    ✅ Правильно
.\stop_bot.ps1     ✅ Правильно

start_bot.ps1      ❌ Неправильно - не работает!
stop_bot.ps1       ❌ Неправильно - не работает!
```

## 🎯 Запуск бота

### Способ 1: PowerShell скрипт (рекомендуется)

```powershell
.\start_bot.ps1
```

### Способ 2: Через cmd

```powershell
cmd /c start_bot.bat
```

### Способ 3: Напрямую через Python

```powershell
.\venv\Scripts\python.exe main.py
```

## 🛑 Остановка бота

### Способ 1: PowerShell скрипт

```powershell
.\stop_bot.ps1
```

### Способ 2: Через cmd

```powershell
cmd /c stop_bot.bat
```

## ❓ Если PowerShell блокирует выполнение

Если видите ошибку "execution of scripts is disabled", выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем попробуйте снова:
```powershell
.\start_bot.ps1
```

## 📋 Проверка

После запуска вы должны увидеть:
```
Инициализация RAG системы...
Загрузка базы знаний...
Запуск Telegram бота...
```

Если видите эти сообщения - бот работает! 🎉

## 🌐 Решение ошибки 403 (регион не поддерживается)

Если видите ошибку `Error code: 403 - unsupported_country_region_territory`:

### Вариант 1: VPN (самый простой!)
1. Установите VPN (NordVPN, ExpressVPN, ProtonVPN и т.д.)
2. Подключитесь к серверу в США или Европе
3. Запустите бота: `.\start_bot_with_vpn.ps1`

**Подробнее:** См. `VPN_SETUP.md`

### Вариант 2: Прокси
1. Получите прокси (см. `PROXY_SERVICES.md`)
2. Добавьте в `.env`: `OPENAI_PROXY=http://proxy:port`
3. Перезапустите бота

**Подробнее:** См. `QUICK_PROXY_FIX.md`

## 🔧 Решение проблем

Если видите ошибку "Conflict: terminated by other getUpdates request":

1. Остановите все экземпляры:
   ```powershell
   .\stop_bot.ps1
   ```

2. Подождите 2-3 секунды

3. Запустите снова:
   ```powershell
   .\start_bot.ps1
   ```

## 📚 Дополнительная информация

- `POWERSHELL_GUIDE.md` - подробное руководство по PowerShell
- `QUICK_FIX.md` - быстрые решения проблем
- `README.md` - общая информация о проекте

